function HamburgerViewModel() {
    this.HamburgerActive = ko.observable(false);

    this.scrollTo = function(element){
		$('html, body').animate({
		    scrollTop: $("#resume").offset().top
		}, 1000);
	}
}

// Activates knockout.js
ko.applyBindings(new HamburgerViewModel());